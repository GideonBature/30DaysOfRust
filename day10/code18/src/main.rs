fn main() {
    let f_name = String::from("Grace");
    let m_name = String::from("Andrew");
    let l_name = String::from("Ajogi");

    let name = format!("{} {} {}", f_name, m_name, l_name);

    println!("{}", name);
}
